{
  "name": "cross-border-settlement-poc",
  "version": "1.0.0",
  "description": "Cross-border settlement system between Rwanda and Kenya",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy-settlement.js --network localhost"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.0",
    "@chainlink/contracts": "^0.6.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.12.6"
  },
  "keywords": [
    "blockchain",
    "cbdc",
    "settlement",
    "cross-border",
    "rwanda",
    "kenya"
  ]
}
